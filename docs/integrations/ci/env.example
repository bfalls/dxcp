# DXCP API base; helper auto-appends /v1 when missing.
DXCP_API_BASE=https://your-dxcp-api.example.com

# Auth0 tenant domain (no scheme preferred, e.g. tenant.us.auth0.com).
GOV_AUTH0_DOMAIN=tenant.us.auth0.com

# Auth0 API audience configured for DXCP API.
GOV_AUTH0_AUDIENCE=https://dxcp-api

# Machine-to-machine client id used by CI registration.
GOV_CI_CLIENT_ID=your-ci-client-id

# CI secret value intentionally omitted.
# GOV_CI_CLIENT_SECRET=<set in CI secret manager>
