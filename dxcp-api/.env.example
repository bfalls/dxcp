### DXCP API local configuration (no secrets)

# Auth0 / OIDC (required for local auth)
DXCP_OIDC_ISSUER=
DXCP_OIDC_AUDIENCE=
# Optional if issuer metadata is not used in your setup
DXCP_OIDC_JWKS_URL=
DXCP_OIDC_ROLES_CLAIM=https://dxcp.example/claims/roles

# UI refresh defaults (optional; defaults shown)
DXCP_UI_DEFAULT_REFRESH_SECONDS=300
DXCP_UI_MIN_REFRESH_SECONDS=60
DXCP_UI_MAX_REFRESH_SECONDS=3600

# Spinnaker (optional for local unless testing engine calls)
DXCP_SPINNAKER_GATE_URL=

# Runtime artifact bucket (optional; required only for version refresh)
DXCP_RUNTIME_ARTIFACT_BUCKET=

