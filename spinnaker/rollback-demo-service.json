{
  "application": "dxcp",
  "name": "rollback-demo-service",
  "description": "Rollback demo-service to previous artifact",
  "keepWaitingPipelines": false,
  "limitConcurrent": true,
  "parameterConfig": [
    {
      "name": "service",
      "required": true
    },
    {
      "name": "version",
      "required": true
    },
    {
      "name": "artifactRef",
      "required": true
    }
  ],
  "stages": [
    {
      "type": "webhook",
      "name": "rollback-artifact",
      "url": "https://demo-runtime.local/rollback",
      "method": "POST",
      "payload": {
        "service": "${ parameters.service }",
        "version": "${ parameters.version }",
        "artifactRef": "${ parameters.artifactRef }"
      }
    }
  ]
}
